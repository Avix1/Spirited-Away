import React from 'react';
import { Button, ButtonText } from './TrailerButtonStyles'

const TrailerButton = () => {
    return (
        <>
            <Button>
                <ButtonText>Assista o trailer</ButtonText>
            </Button>
        </>
    );
};

export default TrailerButton;